{
  "_comment": "Epic N baseline (proposed ADR 0035): the C++ tree-sitter tier as it stands TODAY — likely ceiling, intra-file call resolution only. The cross-file #include edge (total -> shared_util) is honestly `unresolved` and the resolved edge to util.cpp#shared_util is FORBIDDEN; ADR 0035 N-S2 (header-aware resolution) will flip both, updating this golden consciously. The private non-static `value()` is deliberately omitted from `exported` — the coarse `!static` heuristic over-reports it as exported; a later stage may refine access-specifier grading.",
  "symbols": {
    "required": [
      "calc.cpp#Calculator",
      "calc.cpp#run",
      "calc.cpp#step",
      "calc.cpp#value",
      "calc.cpp#helper",
      "calc.cpp#total",
      "calc.cpp#main",
      "util.cpp#shared_util",
      "util.cpp#helper_internal"
    ],
    "forbidden": []
  },
  "exported": {
    "calc.cpp#helper": false,
    "util.cpp#helper_internal": false,
    "calc.cpp#run": true,
    "calc.cpp#total": true,
    "calc.cpp#main": true,
    "util.cpp#shared_util": true
  },
  "edges": {
    "required": [
      { "from": "calc.cpp#run", "to": "calc.cpp#step", "callKind": "direct", "confidence": "likely" },
      { "from": "calc.cpp#step", "to": "calc.cpp#value", "callKind": "direct", "confidence": "likely" },
      { "from": "calc.cpp#total", "to": "calc.cpp#helper", "callKind": "direct", "confidence": "likely" },
      { "from": "calc.cpp#main", "to": "calc.cpp#total", "callKind": "direct", "confidence": "likely" },
      { "from": "util.cpp#shared_util", "to": "util.cpp#helper_internal", "callKind": "direct", "confidence": "likely" },
      { "from": "calc.cpp#total", "to": "unresolved#shared_util", "callKind": "unresolved", "confidence": "unresolved" }
    ],
    "forbidden": [
      { "from": "calc.cpp#total", "to": "util.cpp#shared_util" }
    ]
  },
  "reachability": [
    {
      "target": "main",
      "mustReach": ["calc.cpp#total", "calc.cpp#helper"],
      "mustNotReach": ["util.cpp#helper_internal"]
    }
  ]
}
