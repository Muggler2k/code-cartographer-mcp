<Project Sdk="Microsoft.NET.Sdk">

  <!-- Roslyn sidecar for the C#/VB provider (CAS ADRs 0027/0033). Built on demand by
       src/providers/csharp.ts (`dotnet build -c Release`, memoized per process);
       strictly optional — no .NET SDK means the tree-sitter tier handles C# and the
       heuristic floor handles VB. -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>RoslynAnalyzer</AssemblyName>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp" Version="4.14.0" />
    <PackageReference Include="Microsoft.CodeAnalysis.VisualBasic" Version="4.14.0" />
  </ItemGroup>

</Project>
